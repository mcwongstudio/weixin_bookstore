// 配置项

// const host = 'http://localhost:5757'
const host = 'https://www.honghaha.xyz'
const config = {
  host,
  loginUrl: `${host}/wong/login`,
  userUrl: `${host}/wong/user`,
  openid: ''
}


export default config
